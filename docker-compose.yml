services:
  clientemqtt:
    build: ./clienteMqtt/
    image: cliente_mqtt
    container_name: clientemqtt
    environment:
      - TP=${TP}
      - SERVIDOR=${SERVIDOR}
      - TOPICO1=${TOPICO1}
      - TOPICO2=${TOPICO2}
      - PUBLICAR=${PUBLICAR}
    restart: unless-stopped